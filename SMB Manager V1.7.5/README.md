# SMB Manager V1.7.5

부서를 선택하면 해당 부서에서 사용하는 Windows SMB 공유폴더를 자동으로 연결하는 관리 도구입니다.

## 실행 방법

`Setup.exe`를 실행해 설치합니다.

## 주요 기능

- 부서별 업무폴더 자동 연결
- 전체 연결 해제
- SMB 연결 진단 및 자동 복구
- 저장된 비밀번호를 사용한 진단 후 자동 재연결
- GitHub Releases 기반 실행 시 업데이트 확인 및 자동 업데이트
- SMB 연결 상태 주기 모니터링
- 연결 상태 확인
- 진단 로그 확인
- 서버 IP, 공유폴더 이름, 드라이브 문자, SMB 계정명 설정
- 일반 설정 / 보안 설정 분리
- 앱 내부 관리자 인증 비밀번호 설정
- 최소화/X 버튼 시 트레이 백그라운드 실행
- `Uninstall.exe`를 통한 프로그램 제거

## 참고 사항

- 관리자 인증은 V1.6.6부터 SMB 서버 계정이 아니라 앱 내부 비밀번호로 처리합니다.
- 앱 내부 관리자 비밀번호는 설정 화면의 `보안` 탭에서 변경합니다.
- 최초 관리자 기능 사용 시 앱 내부 관리자 비밀번호를 먼저 설정합니다.
- SMB 계정명은 설정 파일에 DPAPI CurrentUser 방식으로 암호화 저장됩니다.
- 기존 Base64 계정명 설정은 실행 시 자동으로 암호화 저장 형식으로 마이그레이션됩니다.
- 연결 화면에서 `비밀번호 저장`을 선택한 경우에만 SMB 접속 비밀번호를 저장합니다.
- 저장된 SMB 비밀번호는 Windows DPAPI CurrentUser 방식으로 암호화되며 같은 Windows 사용자 계정에서만 복호화할 수 있습니다.
- 제거 시 사용자 설정, 진단 로그, 저장된 비밀번호 삭제 여부를 선택할 수 있습니다.
- 창의 X 버튼 또는 최소화 버튼을 누르면 앱은 종료되지 않고 트레이에서 계속 실행됩니다.
- 실제 종료는 앱 상단의 `종료` 버튼 또는 트레이 메뉴의 `종료`에서 수행합니다.

## 업데이트 이력

### V1.7.5
- GitHub Release asset 탐색이 Release 제목의 `name` 값을 zip 파일명으로 오인하던 문제 수정
- `browser_download_url`에서 zip URL을 직접 찾도록 업데이트 파서 개선
- GitHub Actions가 생성하는 Release 제목을 구버전 업데이트 파서와 호환되도록 보정

### V1.7.4
- 앱 실행 직후 SplitContainer 초기 배치 중 `SplitterDistance` 예외가 발생하던 문제 수정
- SplitterDistance 초기 고정값 제거
- 폼이 실제 크기로 표시된 뒤 반응형 레이아웃을 적용하도록 초기화 순서 변경
- Split 영역이 충분히 확보되지 않은 상태에서는 레이아웃 재계산을 건너뛰도록 보강

### V1.7.3
- 최소화 시 반응형 레이아웃 재계산이 `SplitContainer.SplitterDistance` 예외를 발생시키던 문제 수정
- 최소화 상태에서는 레이아웃 재계산을 건너뛰고 트레이 숨김 처리만 수행하도록 변경
- SplitterDistance 값을 항상 Panel1/Panel2 최소 크기 범위 안으로 보정
- GitHub Actions로 버전 zip을 GitHub Release에 자동 업로드하는 워크플로 추가

### V1.7.2
- GitHub Releases가 아직 생성되지 않은 상태에서 업데이트 확인 시 404 오류가 표시되던 문제 수정
- 릴리즈 미배포 상태를 오류가 아닌 안내 메시지로 표시
- 자동 업데이트 확인 중 릴리즈가 없으면 조용히 건너뛰도록 개선

### V1.7.1
- 업데이트 서버를 공유폴더 `지점공용\App Update`에서 GitHub Releases로 변경
- 공유폴더가 연결되지 않은 상태에서도 최신 릴리즈 확인 가능
- 최신 릴리즈 zip을 다운로드해 압축 해제 후 포함된 `Setup.exe`를 실행하도록 자동 업데이트 방식 개선
- 업데이트 설정 문구를 GitHub 릴리즈 기준으로 정리

### V1.7.0
- 최소화 시 트레이 아이콘으로 숨김 처리
- 창 X 버튼 클릭 시 종료하지 않고 트레이 백그라운드 실행으로 전환
- 트레이 아이콘 더블클릭으로 창 복원
- 트레이 메뉴에 `열기`, `종료` 추가
- 앱 상단 `종료` 버튼은 실제 종료로 유지

### V1.6.9
- 처리되지 않은 WinForms 예외를 `crash.log`에 기록하도록 전역 예외 처리 추가
- JIT 디버깅 창 대신 사용자 메시지와 로그로 오류를 확인할 수 있도록 개선
- 앱 실행 폴더에 남은 이전 `SMB Manager V*.exe` 파일을 시작 시 자동 정리
- 업데이트 설치 시 새 버전 폴더의 `version.ini`를 최신 버전 값으로 정확히 기록

### V1.6.8
- 부서 선택 목록에서 `관리자` 항목이 항상 보이도록 수정
- 관리자 부서 연결 시 연결 버튼에서 앱 내부 관리자 인증을 바로 수행하도록 개선
- 보안 설정 창 종료 후에도 관리자 부서 연결 항목이 사라지지 않도록 수정

### V1.6.7
- 일반 설정과 보안 설정 분리
- 보안 설정 진입 시에만 앱 내부 관리자 인증 수행
- 관리자 비밀번호 해시 저장 방식을 PBKDF2로 강화
- 기존 SHA256 관리자 비밀번호 해시는 성공 인증 시 PBKDF2로 자동 마이그레이션
- SMB 계정명을 DPAPI CurrentUser 암호화 저장으로 변경
- 기존 Base64 SMB 계정명 설정 자동 마이그레이션
- 관리자 비밀번호 초기화 도구 `ResetAdminPassword.exe`를 별도 `.admin-tools` 폴더에 생성
- `ResetAdminPassword.exe`는 일반 배포 zip에 포함하지 않음

### V1.6.6
- 관리자 인증을 SMB 서버 계정 검증에서 앱 내부 비밀번호 검증으로 변경
- 최초 관리자 기능 사용 시 앱 내부 관리자 비밀번호 생성 흐름 추가
- 설정 화면에 `보안` 탭 추가
- 설정에서 관리자 인증 비밀번호 변경 기능 추가
- 관리자 인증 중 SMB 공유 연결 상태에 영향을 주지 않도록 구조 분리

### V1.6.5
- 배포 폴더에서 `Uninstall.exe`를 직접 실행해도 실제 설치 경로를 우선 탐지하도록 수정
- 제거 화면에 실행 경로와 제거 대상 설치 경로를 분리해 표시
- 설치 정보 파일에 `InstallDir` 기록 추가

### V1.6.4
- `Uninstall.exe` 제거 프로그램 추가
- 설치 시 기존 설치 폴더의 이전 `SMB Manager V*.exe` 파일을 먼저 삭제하도록 개선
- 업데이트 설치 시 새 버전 폴더에 README, version.ini, Fonts, Uninstall.exe를 함께 복사
- 새 버전 실행 후 이전 업데이트 버전 폴더를 자동 정리하도록 개선
- 시작 메뉴에 제거 바로가기 생성

### V1.6.3
- 관리자 인증 시 첫 번째 공유폴더만 검사하지 않고 관리자 권한 대상 공유폴더 전체를 순차 검사하도록 개선
- 일반 계정 연결은 정상인데 관리자 인증만 실패하는 경우, PC 네트워크 문제가 아니라 관리자 SMB 계정/권한 문제로 안내하도록 오류 메시지 개선

### V1.6.2
- 관리자 인증 중 기존 SMB 공유폴더 연결을 해제하지 않도록 수정
- 관리자 인증/설정 창의 Noto Sans 폰트 적용 경로 보강
- 폰트 fallback 처리 강화

### V1.6.1
- 앱 본체와 설치 프로그램의 기본 UI 폰트를 Noto Sans CJK KR로 통일
- 배포 패키지와 설치 경로에 `Fonts` 폴더 포함

### V1.6.0
- 사용자가 입력한 비밀번호 암호화 저장 기능 추가
- 진단 및 자동 복구 중 저장된 비밀번호로 자동 재연결 시도
- WinUI3/Fluent 기준에 맞춘 색상, 버튼, 여백, 반응형 화면 구성 개선

### V1.5.0
- 앱 이름에서 지점 표기 제거
- 관리자 인증 후 설정에서 공유폴더 이름, 드라이브 문자, 부서별 SMB 계정명 수정 기능 추가
- 설정 파일에 계정명을 Base64로 저장해 특수문자 입력 안정성 개선
